<Window x:Class="ProyectoMinaELMochito.MainWindow"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
         TextElement.FontWeight="Regular"
         TextElement.FontSize="13"
         TextOptions.TextFormattingMode="Ideal"
         TextOptions.TextRenderingMode="Auto"
         Background="{DynamicResource MaterialDesignPaper}"
         FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProyectoMinaELMochito"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStyle="None">
    <Grid Loaded="CargarDatos">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <!-- Contenido del grid -->
        <Label Content="Sistema De Viajes Internos" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" FontSize="20" Margin="7,10,4,0" Width="253"/>
        <Label Content="Id Empleado:" Grid.Column="0" Grid.Row="2" Margin="30,115,0,7" HorizontalAlignment="Left" FontSize="14" Width="97"/>
        <Label Content="Id Vehiculo:" Grid.Column="1" Grid.Row="2" Margin="0,107,6,9" HorizontalAlignment="Right" FontSize="14" Width="82"/>
        <Label Content="Empleados" Grid.Row="1" Margin="136,20,34,111" FontSize="16"/>
        <Label Content="Vehículos" Grid.Row="1" Margin="243,20,202,111" Grid.ColumnSpan="2" Grid.Column="1" FontSize="16"/>

        <TextBox Name="txtIdEmpleado" Grid.Row="2" Margin="122,99,8,26" FontSize="14" />
        <TextBox Name="txtIdVehiculo" Grid.Row="2" Margin="262,94,116,30" Grid.Column="1" Grid.ColumnSpan="2" FontSize="14"/>

        <DataGrid Name ="dgvEmpleados" HorizontalAlignment="Left" Height="201" Margin="12,57,0,0" Grid.Row="1" VerticalAlignment="Top" Width="349" Grid.ColumnSpan="2" Grid.RowSpan="2">

            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <!--<Setter Property="Background" Value="Linen"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderBrush" Value="LightSteelBlue"/>
                    <Setter Property="BorderThickness" Value="0.8"/>-->
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>

            </DataGrid.Columns>
        </DataGrid>
        <DataGrid Name ="dgvVehiculos" HorizontalAlignment="Left" Height="197" Margin="121,57,0,0" Grid.Row="1" VerticalAlignment="Top" Width="397" Grid.ColumnSpan="2" Grid.RowSpan="2" Grid.Column="1">

            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}">

                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>
        <Button Name="btnSiguiente" Content="Siguiente" Grid.Column="2" Grid.Row="3" Margin="36,7,28,13" Width="180" Height="40" 
                Style="{StaticResource MaterialDesignRaisedButton}"
                materialDesign:ButtonProgressAssist.Value="-1"
                materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                materialDesign:ColorZoneAssist.Background="LightBlue"
                materialDesign:ButtonAssist.CornerRadius="12">
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="DarkTurquoise" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>
        </Button>


    </Grid>
</Window>
